{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Minify css",
            "type": "shell",
            "command": "for file in $(grep -L 'min' CSS_files/*); do python tools/python-min_cli.py \"$file\"; done",
        }
    ]
}