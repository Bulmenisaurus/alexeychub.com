{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Minify css",
            "type": "shell",
            "command": "for file in $(find stylesheets/* ! -name '*min*'); do python tools/minify_css.py \"$file\"; done",
        }
    ]
}