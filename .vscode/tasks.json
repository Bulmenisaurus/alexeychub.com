{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Minify css",
            "type": "shell",
            "command": "for file in stylesheets/*; do python tools/minify_css.py $file; done",
        },
        {
            "label": "Minify this file",
            "type": "shell",
            "command": "python tools/minify_css.py ${relativeFile}"
        }
    ]
}